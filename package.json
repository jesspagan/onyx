{
  "name": "onyx",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "nx": "nx",
    "graph": "nx graph",
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint"
  },
  "devDependencies": {
    "nx": "^20.8.1",
    "typescript": "^5.6.3"
  }
}